<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.a1merbng.tempdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.app.netconnection.MainActivity" />
        <activity android:name="com.app.netconnection.demo.TriangulationDrawableActivity"></activity>
        <activity android:name="com.app.netconnection.demo.LineViewActivity">

        </activity>
        <activity android:name="com.app.netconnection.broadcast.TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.app.netconnection.broadcast.CounterService"
            android:enabled="true" />
    </application>

</manifest>